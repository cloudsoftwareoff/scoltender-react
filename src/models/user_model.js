class User {
    constructor(uid, profileImageUrl, role) {
      this.uid = uid;
      this.profileImageUrl = profileImageUrl;
      this.role = role;
    }
  
    // You can add methods here as needed
  }
  